apiVersion: v1
kind: Service
metadata:
  name: {{ include "mesh-test.fullname" . }}
  labels:
    {{- include "mesh-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mesh-test.selectorLabels" . | nindent 4 }}
