apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mesh-test.fullname" . }}-test-connection"
  labels:
    {{- include "mesh-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mesh-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
